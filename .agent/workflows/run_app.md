---
description: Run the full stack application using Docker Compose
---

1. Start the application services
   Execute the following command in the `docker` directory:
   ```bash
   docker-compose up -d --build
   ```
